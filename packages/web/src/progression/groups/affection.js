// Affection group tree. Direct, gentle, low-key positive interactions.
// pet is the free starter. The `compliment`/"glaze" sycophancy gag was cut
// in the grounded-roster pass (refunded via REMOVED_NODE_COSTS).

import { toolNode } from './_shared.js';

export default [
  toolNode({
    id: 'g.affection.pet', parents: [], cost: 0, toolId: 'pet',
    label: 'pet',
    blurb: 'Stroke the buddy gently. Drag along the body.',
  }),
];
